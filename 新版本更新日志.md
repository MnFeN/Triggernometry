### 2025/08/??

- **1.2.0.751**

  - 鲶鱼精邮差扩展添加实体回调 SetModelPosOffset，设置实体相对于其本身位置显示模型的偏移，可用于变相移动、隐藏一些持续移动中的实体。

  - fix：由于目前 FFXIV 解析插件在新月岛中的 bug，实体 buff 数组可能包含 null 元素，对此加入非空检测

- **1.2.0.750**

  - 更新 7.30 内存签名

  - fix：多行文本框模式下，跨行选中文本并按回车会引发报错

  - MapEffect 的第二个参数默认值从 0 改为 1（使用 Hyperborea 本地进入副本时传入 0 初次无法显示特效，参数疑似与资源加载有关）。

### 2025/07/12

- **1.2.0.749**

  - 字典变量的方法 `.joinvalues(joiner)` `.joinall(kvjoiner, pairjoiner)` 现支持在后面添加任意数量的 key，指定要获取哪些值。
    
    如字典 `a = 1, b = 2, c = 3`，使用 `.joinvalues(",", a, c)` 可得 `1, 3`；使用 `.joinall("=", ", ", a, c)` 可得 `a=1, c=3`。

  - 添加了临时强制开启鲶鱼精邮差的某个模块并执行 Action/Func 的方法，执行后复原。

  - ActorVfxRemove 扫描失败时禁止 ActorVfxCreate，防止绘制出无法消除的实体类特效。

  - fix: 查询实体时，如果查询属性为 Exist，则未找到符合条件的实体时不显示警告。

  - fix: 修复表格动作 “获取全部指定实体的指定属性” 中属性相关的文本描述错误。

### 2025/07/03

- **1.2.0.748**

  - 重构鲶鱼精邮差扩展的模组初始化

  - fix：重启游戏时 ACT 资源未清理干净导致某些内存相关的功能导致游戏崩溃

  - fix：自定义的用户配置界面标题文本错误

### 2025/06/28

- **1.2.0.747**

  - 鲶鱼精邮差扩展增加配置选项，用户可自行开启或禁止某些特效类的回调

### 2025/06/27

- **1.2.0.746**
  
  - fix：由于 OverlayPlugin 目前实体 status 列表存在 bug，临时改为固定使用解析插件，防止获取不到某些 buff 导致触发器出错
    
  - fix：从 FFXIV 解析插件获取的实体 Status 列表不完整

  - PictoACT 增加 VFX 路径格式的检查，防止直接炸游戏

  - 鲶鱼精邮差扩展初始化时检测是否为管理员模式，防止游戏崩溃（根据用户反馈，原因暂时未知）

  - fix：鲶鱼精邮差扩展在移除特效失败时未从 storage 中删除，导致重启后可能会再次尝试移除此不存在的特效

### 2025/06/24

- **1.2.0.745**
  
  - 添加了 StaticVfxRemove 函数的备用签名，以防其他插件 hook 导致扫描不到

  - StaticVfxRemove 未扫描到时，禁止 StaticVfxCreate 绘制静态 VFX

### 2025/06/23

- **1.2.0.744**
  
  - fix：移除 VFX 时多线程访问字典未加锁

- **1.2.0.743**
  
  - 适配国服 7.2

### 2025/06/20

- **1.2.0.742**
  
  - 适配莫灵喵修改版触发器的未加密分享格式

### 2025/06/08

- **1.2.0.741**
  
  - 添加绘图模组 PictoACT，支持以类似高级标点工具箱的格式创建静态特效

  - 绘图添加 Triangulate 模式：以等腰三角形剖分的形式拼接绘制任意多边形

### 2025/05/25

- **1.2.0.740**
  
  - 鲶鱼精邮差扩展重构并整合至触发器中，支持从触发器脚本 sideload 新模组或修改已有 module
    
  - 高级标点工具箱中几何计算相关的内容整合至 Triggernometry.Math 命名空间
